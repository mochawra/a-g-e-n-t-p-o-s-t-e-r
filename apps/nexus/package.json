{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Run from root'",
    "lint": "echo 'Run from root'",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules",
    "typecheck": "echo 'Run from root'",
    "build": "echo 'Run from root'",
    "test": "echo 'Run from root'",
    "db:migrate": "wrangler d1 migrations apply nexus-db --local"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "prettier": "^3.9.4",
    "turbo": "^2.10.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "nanoid": "^3.3.11"
  }
}
