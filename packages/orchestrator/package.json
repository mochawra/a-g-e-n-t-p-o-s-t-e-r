{
  "name": "@posteragent/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 3 — typed agent registry + BaseAgent (memory + identity injection) + task dispatch",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./base-agent": "./src/base-agent.ts",
    "./registry": "./src/registry.ts",
    "./run": "./src/run.ts",
    "./cost": "./src/cost.ts",
    "./types": "./src/types.ts",
    "./wire": "./src/wire.ts",
    "./handlers": "./src/handlers/index.ts",
    "./handlers/real": "./src/handlers/real/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/agent-analytics": "workspace:*",
    "@posteragent/agent-autonome": "workspace:*",
    "@posteragent/agent-publisher": "workspace:*",
    "@posteragent/agent-research": "workspace:*",
    "@posteragent/agent-revenue": "workspace:*",
    "@posteragent/identity": "workspace:*",
    "@posteragent/logger": "workspace:*",
    "@posteragent/memory": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
