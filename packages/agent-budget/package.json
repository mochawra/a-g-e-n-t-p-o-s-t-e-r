{
  "name": "@posteragent/agent-budget",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-902 — Cost management & budget guard. Per-task estimation, daily caps, cheaper-model suggestions, kill-switch.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./pipeline": "./src/pipeline/index.ts",
    "./adapters": "./src/adapters/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
