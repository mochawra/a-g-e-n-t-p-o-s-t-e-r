{
  "name": "@posteragent/agent-autonome",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-900 — Autonome Mode. Hourly: read goals → review progress → identify gaps → queue ≤5 tasks → notify.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./adapters": "./src/adapters/index.ts",
    "./pipeline": "./src/pipeline/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
