{
  "name": "@posteragent/identity",
  "version": "0.1.0",
  "private": true,
  "description": "NEXUS identity layer — SOUL.md injection, per-task journal, NOW scratchpad, persona traits",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./soul": "./src/soul.ts",
    "./journal": "./src/journal.ts",
    "./now": "./src/now.ts",
    "./persona": "./src/persona.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/memory": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
